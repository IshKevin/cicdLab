- hosts: app
  become: true

  tasks:
    - name: Pull image
      command: docker pull ishikevin/flask-app

    - name: Remove old container
      command: docker rm -f flask-app || true

    - name: Run container
      command: docker run -d -p 5000:5000 --name flask-app ishikevin/flask-app